#include <bits/stdc++.h>
#define rep(i, n) for(int i=0;i<(n); i++)
#define sz(x) int(x.size())
using namespace std;


int main() {
    int i = 0;
    while(true){
        cout << i++ << endl;
    }

    return 0;
}
